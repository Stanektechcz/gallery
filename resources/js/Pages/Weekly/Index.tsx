import AppLayout from '@/Layouts/AppLayout';
import { Head, Link } from '@inertiajs/react';
import axios from 'axios';
import { FormEvent, useEffect, useState } from 'react';

interface Overview { period:string[]; events:any[]; open_tasks:any[]; travel_inbox:any[]; rediscover:any[]; }
export default function Weekly() { const [data,setData]=useState<Overview|null>(null); const [spaceId,setSpaceId]=useState<number|null>(null); const [capsule,setCapsule]=useState({title:'',deliver_at:''}); const [saved,setSaved]=useState(false); useEffect(()=>{Promise.all([axios.get('/api/v1/calendar/weekly-overview'),axios.get('/api/v1/calendar/events')]).then(([overview,calendar])=>{setData(overview.data);setSpaceId(calendar.data.spaces?.[0]?.id??null);}).catch(()=>setData({period:[],events:[],open_tasks:[],travel_inbox:[],rediscover:[]}));},[]); const seal=async(e:FormEvent)=>{e.preventDefault();if(!spaceId)return;await axios.post('/api/v1/calendar/time-capsules',{gallery_space_id:spaceId,...capsule});setCapsule({title:'',deliver_at:''});setSaved(true);setTimeout(()=>setSaved(false),2500);}; return <AppLayout><Head title="Týdenní přehled"/><main className="mx-auto max-w-5xl p-4 sm:p-6"><div className="flex justify-between"><div><h1 className="text-xl font-semibold text-white">Náš týden</h1><p className="mt-1 text-sm text-[var(--color-text-secondary)]">Jeden klidný společný přehled plánů, příprav a vzpomínek.</p></div><Link href="/calendar" className="text-sm text-[var(--color-accent)]">Kalendář</Link></div><div className="mt-5 grid gap-4 md:grid-cols-3">{[['Akce',data?.events??[],(x:any)=>x.title],['Otevřené úkoly',data?.open_tasks??[],(x:any)=>x.title],['Cestovní inbox',data?.travel_inbox??[],(x:any)=>x.title]].map(([title,items,render]:any)=><section key={title} className="rounded-2xl border border-[var(--color-border)] bg-[var(--color-bg-card)] p-4"><h2 className="font-semibold text-white">{title}</h2><div className="mt-3 space-y-2">{items.length?items.map((item:any)=><p className="rounded-lg bg-black/10 p-2 text-sm text-[var(--color-text-secondary)]" key={item.id??item.uuid}>{render(item)}</p>):<p className="text-sm text-[var(--color-text-secondary)]">Nic naléhavého.</p>}</div></section>)}</div><section className="mt-4 rounded-2xl border border-[var(--color-border)] bg-[var(--color-bg-card)] p-4"><h2 className="font-semibold text-white">Co jsme ještě neviděli</h2><p className="mt-1 text-sm text-[var(--color-text-secondary)]">Starší, neoblíbené fotografie k opětovnému objevení. Výběr zůstává pouze ve vašem prostoru.</p><div className="mt-3 flex flex-wrap gap-2">{(data?.rediscover??[]).map((item:any)=><Link key={item.uuid} href={`/media/${item.uuid}`} className="rounded-lg border border-[var(--color-border)] px-3 py-2 text-sm text-white">{item.display_title||'Vzpomínka'}</Link>)}</div></section><section className="mt-4 rounded-2xl border border-[var(--color-border)] bg-[var(--color-bg-card)] p-4"><h2 className="font-semibold text-white">Časová kapsle</h2><p className="mt-1 text-sm text-[var(--color-text-secondary)]">Zapečetěte krátký vzkaz; doručí se jako soukromé upozornění ve zvolený čas.</p><form onSubmit={seal} className="mt-3 flex flex-col gap-2 sm:flex-row"><input required value={capsule.title} onChange={e=>setCapsule({...capsule,title:e.target.value})} placeholder="Název vzpomínky" className="min-h-10 flex-1 rounded-lg border border-[var(--color-border)] bg-black/10 px-3 text-sm text-white"/><input required type="datetime-local" value={capsule.deliver_at} onChange={e=>setCapsule({...capsule,deliver_at:e.target.value})} className="min-h-10 rounded-lg border border-[var(--color-border)] bg-black/10 px-3 text-sm text-white"/><button className="rounded-lg bg-[var(--color-accent)] px-3 text-sm text-white">{saved?'Uloženo ✓':'Zapečetit'}</button></form></section></main></AppLayout>; }
